todo：

del * 每个主题一个页面，文件存为路径 id.txt
  // 不加日期 yyyy/mm， 因为总主题数不会太多，几万？

* 使用 wiki 语法，格式化。

* 定期查询，显示光标所在的某个词汇在多少个文档中出现，显示前5个；
  避免创建相近的重复主题文件。只显示第一行；
  点击链接后自动创建尚不存在的主题。

* 后台存储结构不能使用文件，还是用单文件大json, 每次重写，这样也可以git管理。

* 可以与原 mynotes 兼容。原 notes 都成为无主题文本，等待赋予主题。
  每个主题可以比较大，一篇文章，不是仅仅短小的notes。
  仍支持短小的微博式 note