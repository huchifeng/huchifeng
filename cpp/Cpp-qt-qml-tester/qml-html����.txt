html 可以给 qml 传递对象 {a:[1,2,3]}，但不能传数组 [1,2,3]

从qml返回的对象中的数组，例如 qml.f().arr 需要 [].concat(arr); 否则可能发现不能进行 splice 等修改操作。

html 里可以对 qt/qml 对象 x 添加属性 x.yyyy = "..";
但不能覆盖内部已有的属性, 即搜索时可能还是优先搜索 qml 定义的方法；