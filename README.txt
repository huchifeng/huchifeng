===20110627===
更改:
mygit 除二进制串整数id存取外，还应该有 key-value 存取。
value 不做hash，直接字符串比较判断重复。
value 本身可能就是一个 仓库id，或者是一个 json 串，引用仓库id；
value 应该是单行的 json，不应该包含数组，长度应该很小。可以在列表时直接显示。
如果 value 是单个整数，说明内容已作为二进制串存入，是id；

添加类型字段，
[整数id hash 类型 内容]
b bin类型，空字段
j json类型(UTF8)
代替key-value

===20110627===
更改:
简化mygit仓库，只含一种内容，就是二进制串。
mygit.exe 只提供最简单的操作：
 * 存入一个二进制串，得到64位整数id；重复存入的内容不增加仓库大小。
 * 根据一个64为整数id提取一个二进制串
 * 列表

而文件名，提交注释等信息，包含在二进制串内，由脚本来处理。
需要 mygit.js
用作备份时，则是 bakup.js 等。
根据需要灵活定制，使用相同的存储层。


===20110614===
mygit 的主要 idea:

1. 整个仓库一个文件
从 CVS,SVN 发展到 GIT 等分布式版本控制库，
仓库从每个目录下一个 CVS 目录或 .svn 目录，变为整个工作目录的根目录下只有一个 .git；
下一步仓库应该演变为整个仓库一个文件，更为方便。

2. 使用64位整数作为仓库中对象的id
使用了hash值，但仅用于快速的排除重复内容的辅助手段，而不作为对象的id。
在远程访问，需要快速合并更改之类的情况，可以考虑假设hash值相同的内容也相同。

3. 仅提供一个最简单的存储层，不实现高级功能。
例如每个文件都是完整的保存，没有差分存储。

可用于文件备份，同样内容反复添加对库没有影响，
添加相同内容但不同文件名的文件，仅记录新的符号名。

可以添加相同文件名不同内容的文件，所以应该也可以用于版本控制，
需要编写脚本或其他语言的程序，以实现整个目录的检入检出等版本管理需要的功能。


目前仓库中包括两种记录
1. 内容： id+hash值+文件内容
2. 符号名：id+所引用的内容对象的id+符号名+建立日期
用于文件备份已经可以了。
用于项目的版本控制，可能需要添加用于目录的对象，以及用于提交注释的对象。
还需要开发仓库之间的内容交换功能。



